# KOBIS 박스오피스 추적 대시보드 — 플레이북

그린랜드2(2026-07 개봉) 프로젝트에서 얻은 방법론·실패모드·해법을 다음 영화에 재사용하기 위한 문서.
매시간 KOBIS 예매/회원통계를 수집해 공개 대시보드(https://jaywhyyang.github.io/greenland2/)에 예측·코멘트를 올리는 파이프라인.

---

## 0. 구성 한눈에
- **수집(OS 작업, 세션 무관·데이터 생명줄)**: `kobis_greenland2.py`(실시간 예매·순위), `member_auto.py`(회원통계=일별 관객), `schedule_ingest.py`(배급 시간표 xlsx→편성 좌석/회차/지역).
- **데이터 파일**: `member_snapshots.csv`(수집시각·날짜·관객수(당일 러닝)·누적관객), `greenland2_hourly.csv`(실시간 순위/예매율/누적), `schedule_history.json`(날짜별 편성 좌석·회차·체인·지역·시간대).
- **엔진**: `build_dashboard.py` → `index.html` 생성. 핵심 함수 `top_forecast`(오늘 최종), `precise_forecast`(개봉 최종 총관객), `sixtyk_countdown`(상단 배너).
- **코멘트**: `ai_comment.json`(text/lifetime/weekend/decomp 4필드) — 세션 cron이 30분마다 갱신.

## 1. 두 데이터의 성격 (혼동 금지)
- **회원통계 관객수** = 오늘 상영분의 '예매+발권' 누적. "오늘 최종의 현재 확정분"에 가깝다. **오늘 최종 예측의 기준**.
- **실시간 예매율/순위**(greenland2_hourly) = 예측 아님. 순위·예매율 추세·붐업 여부 판단용. **누적관객 컬럼은 하루 지연**(어제 확정치 고정, 당일치는 다음날 반영).
- 규칙: '오늘 최종' 헤드라인은 회원 기반. 실시간을 최종으로 착각 금지(최종<현재값이면 버그).

## 2. 예측 모델 로직
- **오늘 최종(top_forecast)**: ① 같은 요일유형·공통시각 역산 + ② 당일 곡선 → 평균(정밀). 표본 부족/새벽엔 ③ **편성좌석 앵커**(오늘 좌석 × 직전 완료일 좌석판매율, 요일보정)로 대체. 물리 좌석 상한 캡.
- **개봉 최종(precise_forecast)**: 마지막 완료일 누적에서 남은 일자를 하루씩 적산. 각 미래일 = 지난주 동일요일 실측 × 유지율(당주 캘리브레이션, 이후 감쇠) × 편성삭감 계수. **오늘(i=0)은 top_forecast 실측 진행분으로 확정**. 미래일은 **편성좌석 상한**(seats×최근판매율×결집계수)으로 캡.
- **벤치 보정**: LIFETIME_FRAC(누적÷최종 비율)을 최근접 comp 실측곡선으로 재보정. 그린랜드2는 너자2(최종 59,179, 6만 미달) 기준. → **그린랜드2 자신도 이제 comp**(benchmarks/greenland2/metrics.json).

## 3. 실패모드와 해법 (하드코딩된 가드 — 재사용 핵심)
1. **요일유형 왜곡**: 토요일을 금요일과 비교하면 왜곡. → `member_daycompare(ref_mode)`: 예측=sameweekday, 어제대비 차트=prev 분리.
2. **수집 공백 → 유지율 과대**: 지난주 수집 빈 시각에 오늘 최신값을 대면 비율 폭등. → **공통 관측시각**으로만 비교.
3. **편성 절벽(3주차 대작 개봉)**: 좌석이 90%+ 삭감되면 '수요' 아닌 '좌석'이 관객 천장. → 미래일에 **좌석 상한** 적용. 배급이 시간표 안 주면 마지막 편성좌석에서 하루 15% 감쇠 가정.
4. **catch-up 블립(급등)**: 밤새 미반영분이 한 구간에 몰려 8→388처럼 튄 뒤 평탄. 곡선 외삽이 이를 '성장'으로 오해→과대(1,328). → **급등후평탄 감지**: 큰 점프(>현재의 30%) 뒤 마지막 구간 평탄이면 est를 현재값×1.4로 제한. 교차검증: 실시간 예매율 0.0%면 실수요 아님.
5. **새벽 붕괴**: 새벽엔 선예매만 평탄→시간대 투영이 그 값(≈선예매)으로 붕괴. → now<10시면 편성좌석 앵커 유지.
6. **단일 스냅샷 붕괴**: 수집 재개 직후 1개 스냅샷이면 개장 베이스라인 상쇄로 현재값 근처로 붕괴(400). → 표본<3 또는 관측폭<150분이면 편성좌석 앵커.
7. **OneDrive/백신 git lock**: push 실패 시 `(git push || (sleep 3 && git push))` 재시도 + fetch로 SYNCED 확인.
8. **cp949 콘솔 인코딩**: 한글/이모지 print 최소화. 코멘트 텍스트는 별도 .py에서 json.dump로 파일 기록.

## 4. 분석 레시피
- **프로모 ROI(DiD)**: 프로모 안 한 체인(CGV+메가)을 대조군으로 순증 측정. 그린랜드2 롯데 5,000장 → 순증 ~5,280(1:1, 스노볼 없음). 500장 추가는 순증 보장 불확실로 미집행.
- **지역/체인 비중**: 편성 좌석의 지역 분포로 서울/수도권 의존도 산출(그린랜드2 서울 42%·수도권 67%). 대작 개봉 시 수도권 편성이 먼저 빠져 취약.

## 5. 사이트 문구 원칙
- 공개 대시보드 문구·수치 = **수집 데이터 + 공개 정보 + 유추만**. 배급사 내부 대화발 비공개 인텔 금지(사이트/내부 이중관리). 편성 삭감·체인 종영은 '제공된 시간표 데이터'면 표기 가능.
- 톤: 담백·중립·존댓말, 쉬운말·간결, 우쭈주/안심 편향 금지. 헤더 시각=실제 시각(예상시각 금지). 숫자 정합(오늘최종→마감누적→최종전망).

## 6. 다음 영화 세팅 체크리스트
1. `film_config.json` 값 교체(movie_cd·keyword·title·open_date·target·benchmarks).
2. 스크립트 상수 교체: `member_auto.py`의 `MOVIE_CD`, `kobis_greenland2.py`의 `MOVIE_KEYWORD`, `build_dashboard.py`의 개봉일·LIFETIME_FRAC(새 comp로 재보정)·지역/체인 비중.
3. 파일명/경로의 'greenland2' → 새 영화 슬러그(csv·json·GitHub Pages 리포).
4. OS 작업 스케줄러에 수집기 3종 등록(세션 무관 실행). 회원수집 멈춤 시 KOBIS 로그인/비번 만료 점검.
5. 벤치마크 라이브러리(`benchmarks/`)에서 최근접 comp 선택 → LIFETIME_FRAC·요일판매율 시드로 사용.

## 7. 이 프로젝트 자산 위치
- `benchmarks/greenland2/` — 시계열 원본 동결 + `metrics.json`(일별 곡선·요일판매율·lifetime frac·지역/체인 비중·프로모 DiD·편성절벽 사례). **⚠️ 7/19경 완전 종영 후 최종 수치로 1회 갱신** 필요(현재 final_cum은 진행 중 잠정 54,947).
- `~/.claude/.../memory/` — 톤·벤치보정·사이트문구·방법론 메모리.
